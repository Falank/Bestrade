<div>
    @model List<Bestrade.Models.Supplier>
    @{
        ViewBag.Title = "Supplier";
    }
    <table class="table">
        <tr>
            <th>Suppliers</th>
        </tr>
        <tr>
            <td>
                <ul class="list-unstyled">
                    @foreach (var a in Model)
                    {
                        <li>@Html.ActionLink(a.supplier, "PurchaseFromSupplier", new { supplier = a.supplier })</li>
                    }
                </ul>
            </td>
        </tr>
    </table>
</div>

